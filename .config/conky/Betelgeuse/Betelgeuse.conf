conky.config = {
--==============================================================================

--  Created : 2021/Oct/25
--  This theme is for conky version 1.10.8 or newer
-- 
--  BETELGEUSE
--  ( A part of Orion Conky themes pack )
--
--  author  : Closebox73
--  version : 2.6
--  variant : Quicksand & Fahrenheit
--  license : Distributed under the terms of GPLv3

--==============================================================================
-- Custom Settings --
-- Change monitor
  xinerama_head = 1,

-- Size and Position settings --
  alignment = 'top_middle',
  gap_x = 0,
  gap_y = -40,
  maximum_width = 1000,
  minimum_height = 200,
  minimum_width = 1000,
  
-- Text settings --
  use_xft = true,
  override_utf8_locale = true,
  font = 'Bebas Neue:size=9',
  
-- Color Settings --
  default_color = 'white',
  default_outline_color = 'white',
  default_shade_color = 'white',
  color1 = 'FFFFFF',
  
-- Window Settings --
  background = false,
  border_width = 1,
  draw_borders = false,
  draw_blended = false,
  draw_graph_borders = false,
  draw_outline = false,
  draw_shades = false,
  own_window = true,
  own_window_colour = '000000',
  own_window_class = 'Conky',
  own_window_argb_visual = false,
  own_window_type = 'desktop',
  own_window_transparent = true,
  own_window_hints = 'undecorated,below,sticky,skip_taskbar,skip_pager',
  stippled_borders = 0,
  
-- Others --
  cpu_avg_samples = 2,
  net_avg_samples = 2,
  double_buffer = true,
  out_to_console = false,
  out_to_stderr = false,
  extra_newline = false,
  update_interval = 1,
  uppercase = false,
  use_spacer = 'none',
  show_graph_scale = false,
  show_graph_range = false,
  lua_load = '~/.config/conky/Betelgeuse/scripts/text.lua',
  lua_draw_hook_pre = 'draw_text',
}

conky.text = [[
${execi 300 ~/.config/conky/Betelgeuse/scripts/weather-v2.0.sh}\
${execi 100 ~/.config/conky/Betelgeuse/scripts/weather-icon.sh white $(cat ~/.cache/weather.json | jq -r '.weather[0].icon')}\
${image ~/.cache/weather-icon.png -p 245,115 -s 38x38}\
${image ~/.config/conky/Betelgeuse/res/line.png -p 300,0 -s 3x180}\
${image ~/.config/conky/Betelgeuse/res/line.png -p 500,0 -s 3x180}\
${image ~/.config/conky/Betelgeuse/res/line.png -p 700,0 -s 3x180}\
${Alignr 720}${voffset 20}${font Quicksand:Bold:size=38}${time %A}${font}
${Alignr 720}${voffset 5}${font Quicksand:Bold:size=13}${time %d %B}${font}
${Alignr 770}${voffset 10}${font Quicksand:size=9}The Weather in ${execi 100 cat ~/.cache/weather.json | jq -r '.name'} is ${execi 100 cat ~/.cache/weather.json | jq -r '.weather[0].main'}. ${execi 100 cat ~/.cache/weather.json | jq '.main.temp' | awk '{print int($1+0.5)}'}°F
${Alignr 770}${voffset 1}${font Quicksand:size=9}wind speed in your location is ${execi 100 (cat ~/.cache/weather.json | jq '.wind.speed')} mph
${Alignr 770}${voffset 1}${font Quicksand:size=9}and Humidity is ${execi 100 (cat ~/.cache/weather.json | jq '.main.humidity')}%${font}
${offset 380}${voffset -25}${color1}${font Quicksand:Bold:size 22}HOUR${goto 577}MINUTE${goto 770}SECOND 
]]
